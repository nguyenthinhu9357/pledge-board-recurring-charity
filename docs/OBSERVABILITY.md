# Observability

Monitor app availability, wallet connection failures, simulation errors, contract
submission latency, duplicate hashes, funded pledges past due, and failed release or
refund attempts. Logs may contain public addresses, pledge IDs, and hashes but never
secret keys or signed envelopes.

Alert on repeated contract errors or a funded pledge that cannot reach an expected
terminal state. User-rejected signatures are normal product events.
